# Handoff - 2026-04-10

## What was completed in this thread

### 1. Residual control-plane implementation work
- Tasks 1-8 from `.sisyphus/plans/residual-control-plane-closure.md` were implemented and verified earlier in this thread.
- Core runtime improvements already landed before the auth-carrier pivot:
  - canonical `activeHop`
  - pause/resume override semantics fixed to between-hop behavior
  - popup/overlay alignment toward canonical truth
  - unified observation sample
  - canonical target binding
  - settled-reply tightening
  - timeout / observation taxonomy split
  - unit regressions for these semantics

### 2. Browser auth carrier baseline was changed
The repo baseline was shifted from old auth-export / profile-copy assumptions to:

- **Primary**: Playwright Chromium + replayed `storageState`
- **Fallback**: real browser profile + CDP attach

Key changes:
- Added shared browser strategy layer in `scripts/_playwright-bridge-helpers.mjs`
- Added:
  - `scripts/cdp-attach-smoke.mjs`
  - `scripts/launch-chatgpt-cdp-browser.mjs`
  - `scripts/storage-state-auth-smoke.mjs`
  - `scripts/export-storage-state-from-cdp.mjs`
- Updated docs:
  - `docs/auth.md`
  - `docs/testing.md`
  - `GSTACK_AUTH_CARRIER_EXPERIMENT.md` (rewritten into current baseline doc)
- `package.json` script surface was reoriented:
  - unsuffixed `test:*` now point at the storageState-backed primary path
  - `:anon` scripts remain for compatibility
  - `auth:export` now points to CDP export of storageState
  - `auth:export:legacy` keeps the old export path
- Deleted stale duplicate script:
  - `scripts/export-chatgpt-auth-cdp.mjs`

### 3. Carrier validation results
These were successfully validated in this thread:

- `pnpm test` ✅
- `pnpm run typecheck` ✅
- `pnpm run build` ✅
- `pnpm run auth:verify -- --auth-state playwright/.auth/chatgpt.cdp.storage.json` ✅
- `node scripts/storage-state-auth-smoke.mjs --replay-only` ✅

Important proven fact:
- Exporting `storageState({ indexedDB: true })` from a real manually logged-in browser via CDP and replaying it into **Playwright Chromium + extension** succeeded.
- This disproves the earlier assumption that WSL Chrome was the only viable login-state carrier.

## Current repo-local state at the moment of handoff

### Most recent pushed commit
- `5c4d294` — browser auth carrier model switch

### Current working tree is dirty
At the time of this handoff, uncommitted work exists. The thread got dirty again after the carrier-model commit.

Known dirty files seen during the last steps include:
- `.gitignore`
- `.sisyphus/boulder.json`
- `repomix-output.xml`
- `scripts/_playwright-bridge-helpers.mjs`
- `scripts/real-hop-playwright.mjs`
- `scripts/e2e-bridge-playwright.mjs`
- `scripts/semi-bridge-playwright.mjs`

Do **not** assume the dirty tree is safe to commit as-is.

## Most important current findings

### A. Attach fallback browser did not actually have the extension loaded
When attaching to the user-provided Google Chrome instance:
- `serviceWorkers: []`
- overlay count on both thread pages was `0`
- `real-hop` failed at `ensureOverlay()`

So:
- attach fallback currently proves browser/page access
- it does **not** currently prove extension availability
- do **not** use attach fallback as the main plugin-testing lane without revalidating extension load

### B. Chromium + storageState main lane was real, but login detection had false positives
User correctly caught that some earlier auth checks were too loose.
Problem:
- auth checkers treated “composer-like UI exists” as sufficient proof of logged-in state
- this likely caused false positives on login pages / intermediate states

Files involved:
- `scripts/storage-state-auth-smoke.mjs`
- `scripts/verify-chatgpt-auth.mjs`
- `scripts/export-chatgpt-auth.mjs`
- `scripts/_playwright-bridge-helpers.mjs`

This area still needs cleanup and unification.

### C. real-hop resumed on a manual skip-bootstrap path
With user-prepared live pages under Chromium + extension, a `real-hop -- --skip-bootstrap` run progressed and passed the authenticity gate.

Important implication:
- carrier is no longer the main blocker
- business validation can move forward if page preparation is stable

### D. e2e/semi are still unstable on auth-backed provided-thread runs
Latest stable diagnosis from the end of the thread:

- `e2e --scenario resume-default --url-a ... --url-b ...` no longer blocks on anonymous seed classification
- it now fails because **provided thread continuity is being lost**, and/or pages drift back to root during/after binding or auth-backed setup

Observed diagnostics showed:
- `pageAUrl: https://chatgpt.com/`
- `pageBUrl: https://chatgpt.com/`
- `pageAStillThread: false`
- `pageBStillThread: false`

Meaning:
- the harness reaches bind
- but the pages no longer remain on the expected thread URLs by the time post-bind readiness is checked

This is where the thread stopped.

## Last changes made before stopping

These repo-local runtime/harness changes were made but **not yet cleanly finalized** in a fresh thread:

1. `real-hop` auth-backed root-only path was changed to:
   - prefer existing assistant payload
   - avoid anonymous seed path on authenticated root pages
   - classify:
     - `auth_carrier_lost_before_seed`
     - `auth_carrier_lost_during_seed`
     - `auth_root_source_payload_missing`

2. `e2e` / `semi` were partially updated to:
   - use auth-aware source seed helpers
   - avoid anonymous seed classification on auth-backed runs
   - add post-bind auth checks and provided-thread reuse attempts
   - remove sessionStorage restore/reload in the provided-thread path

3. A further patch added provided-thread continuity diagnostics:
   - `auth_provided_thread_lost_after_binding`

These changes are the most likely place to continue from.

## What the next thread should do first

### 1. Re-establish clean ground truth on dirty files
Before doing more feature work:
- inspect current `git status`
- read current dirty diffs for:
  - `scripts/_playwright-bridge-helpers.mjs`
  - `scripts/real-hop-playwright.mjs`
  - `scripts/e2e-bridge-playwright.mjs`
  - `scripts/semi-bridge-playwright.mjs`
- decide what is real progress vs thread noise

### 2. Fix auth detection rigorously
Unify auth checks so they do **not** use loose composer-only heuristics.
Need stronger distinction between:
- actual authenticated ChatGPT UI
- auth/login page with input controls
- root landing with partial UI

Target scripts:
- `scripts/storage-state-auth-smoke.mjs`
- `scripts/verify-chatgpt-auth.mjs`
- `scripts/export-chatgpt-auth.mjs`
- `scripts/_playwright-bridge-helpers.mjs`

### 3. Continue from the real business blocker, not auth carrier
Carrier is mostly solved.
Next mainline blocker is:
- **provided-thread continuity / post-bind drift** on storageState-backed e2e/semi

This is the most likely next work area:
- `scripts/e2e-bridge-playwright.mjs`
- `scripts/semi-bridge-playwright.mjs`
- possibly minimal shared helpers in `_playwright-bridge-helpers.mjs`

### 4. Do not reopen old carrier debates
Already established:
- primary candidate: Chromium + storageState replay
- fallback: real browser + CDP attach
- old profile-copy export path is legacy

## Suggested next-thread objective

Use a narrow objective like:

> Stabilize business validation on the Chromium + storageState primary baseline by fixing auth-backed provided-thread continuity and eliminating auth-detection false positives.

## Minimal command/evidence set worth preserving

### Good signals already established
- `pnpm run auth:export`
- `pnpm run auth:verify -- --auth-state playwright/.auth/chatgpt.cdp.storage.json`
- `node scripts/storage-state-auth-smoke.mjs --replay-only`

### Evidence files worth consulting
- `tmp/storage-state-smoke/summary.json`
- latest `tmp/auth-verify-*.json`
- `tmp/real-hop-2026-04-10T10-47-11-542Z/` (successful manual skip-bootstrap authenticity pass)
- latest `tmp/e2e-resume-default.txt`
- latest `tmp/e2e-source-seed.txt`

## Final note

This thread reached a point where continuing in-place risked mixing:
- solved carrier work
- partially corrected auth detection
- unstable provided-thread business harness behavior

Starting a fresh thread from this handoff is the right move.
