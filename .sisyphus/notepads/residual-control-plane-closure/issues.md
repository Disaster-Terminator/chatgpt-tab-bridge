2026-04-08 Task 1: no blocking implementation issues encountered during the canonical hop-contract change.
2026-04-08 Task 1: existing popup-model fallback logic still exists outside this task scope; background observation now prefers `activeHop` when present without broadening popup semantics.
2026-04-08 Task 2: TypeScript initially rejected the fresh-hop helper as a type predicate because the false branch collapsed `RuntimeHopTruth` to `never`; switching the helper to a boolean guard preserved the intended control flow without widening scope.
2026-04-08 Task 5: background sample unwrapping initially returned the broader `ThreadActivityResponse` union on failure; narrowing it to an explicit `{ ok: false, error }` wrapper resolved the type mismatch without broadening the transport change.
2026-04-09 Task 7: tests import compiled background/runtime fixtures before source fallbacks, so Task 7 verification needed a rebuild before the targeted polling/background tests exercised the new settle predicate and stop taxonomy.
2026-04-09 Task 9 harness baseline shift: after removing auth-file preflight, the targeted browser runs no longer fail on missing auth or unsupported root URLs; they now reach page-fact failures in `ensureSourceAssistantSeed()` / settled-assistant observation on anonymous root pages.
2026-04-09 Task 9 harness baseline shift: `pnpm run test:real-hop -- --root-only` advanced through anonymous binding/startup and produced page-fact evidence artifacts, but the run still died later with `page.evaluate: Target page, context or browser has been closed` during verification.
2026-04-09 Task 9 harness semantics: source seeding on anonymous root pages remains intermittent in this environment even after aligning the runner flow toward `real-hop`; `resume-with-override-a` still hit the source-seed failure path on the latest rerun.
2026-04-09 Task 9 harness semantics: `resume-default` no longer fails immediately on the first `waiting_reply` sample, but the scenario still fails later around pause/stop timing, indicating remaining browser-harness semantics work rather than product-runtime regression.
2026-04-09 Task 9 harness restructuring: the new serial `task9-suite` no longer retries anonymous source seeding per branch; a single shared seed attempt can still be blocked up front by login diversion, now surfaced as `anonymous_seed_blocked_by_login_diversion` instead of a generic scenario failure.
2026-04-09 Task 10 verification: `pnpm run test:real-hop -- --root-only` no longer continues past anonymous source-seed loss into a fake beat-4 timeout FAIL; in the current environment it stops early as `BLOCKED` with `anonymous_seed_blocked_by_login_diversion`, zero runtime events, and popup/runtime still at `ready` / `idle`.
