2026-04-08 Task 1: introduced minimal `activeHop` contract on `RuntimeState` with `sourceRole`, `targetRole`, `targetTabId`, `round`, `hopId`, and `stage`.
2026-04-08 Task 1: kept `nextHopSource` and `nextHopOverride` intact for existing control/UI semantics, but moved execution truth for running hops into the persisted reducer-owned contract.
2026-04-08 Task 1: start/resume/hop_completed rebuild pending `activeHop`; background promotes it to `verifying` and `waiting_reply` by writing the hop id back into persisted state.
2026-04-08 Task 2: `reduceResume()` now distinguishes a fresh unclaimed pending hop (`stage: pending`, `hopId: null`, `round === state.round + 1`) from an already claimed hop before applying override intent.
2026-04-08 Task 2: `resumeSession()` now starts the relay loop from the just-resumed snapshot so the preserved `activeHop` chosen by the reducer is the one background consumes first.
2026-04-08 Task 5: introduced a fact-only `TargetObservationSample` contract and embedded it in the existing target activity transport so baseline capture and verification polling can read one coherent observation window without moving acceptance logic into content-script.
2026-04-08 Task 5: kept settle polling on `GET_ASSISTANT_SNAPSHOT` and preserved `relay-core` verification / acceptance semantics; this slice only swapped baseline + verification transport.
2026-04-09 Task 6: persisted hop-scoped progress on `activeHop` so paused `verifying` / `waiting_reply` work can resume verification or reply waiting without replaying preflight/send.
2026-04-09 Task 6: bound verification and waiting to `activeHop.targetTabId` and added background-side target observation classification (`correct_target`, `wrong_target`, `stale_target`, `unreachable_target`) so post-dispatch polling no longer follows binding drift as normal progress.
2026-04-09 Task 7: moved reply settlement onto the unified `TargetObservationSample` so the settle predicate reads target identity, latest assistant facts, and `generating` from the same observation sample instead of an assistant-only snapshot.
2026-04-09 Task 7: post-acceptance reply wait now stops with observation-class reasons (`wrong_target`, `stale_target`, `unreachable_target`) when the canonical target check fails, while `hop_timeout` remains reserved for correct-target observations that never satisfy the stronger settle predicate.
2026-04-09 Task 9 harness baseline shift: Playwright auth became opt-in instead of implicit-by-default; anonymous/live-session root pages are now the default browser verification baseline unless `--auth-state` or `--session-state` is explicitly provided.
2026-04-09 Task 9 harness semantics: browser-side pause targeting now reads popup runtime `activeHop` to distinguish true between-hop windows from claimed hops, rather than assuming popup next-hop text alone is authoritative.
2026-04-09 Task 9 harness semantics: `waitForAcceptedHop()` no longer treats the first `waiting_reply` observation as an immediate failure, because page-fact acceptance can lag behind runtime step transitions in round-2 continuation flows.
2026-04-09 Task 10 harness semantics: `scripts/real-hop-playwright.mjs` now treats anonymous source-seed failure before any valid first-hop proof attempt as a blocker boundary, reusing the shared `HarnessBlockerError` taxonomy instead of continuing into a misleading beat-4 FAIL.
2026-04-09 Task 10 harness semantics: `real-hop` still preserves page-fact-first authenticity once proof is in play; only pre-proof anonymous seed/login-diversion instability is classified as `BLOCKED`, while post-start acceptance contradictions remain `FAIL`.
2026-04-10 Task 12 harness semantics: auth-backed `pnpm run test:real-hop -- --root-only` now prefers an already-existing assistant reply on page A as the first-hop payload source and does not auto-seed the authenticated root page during pre-proof setup.
